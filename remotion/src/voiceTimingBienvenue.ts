/**
 * Minutage réel de la voix off de la vidéo « Bienvenue » (après achat).
 * Mesuré avec ffprobe sur les 5 segments MP3.
 */
export const VOICE_LEAD_SECONDS = 1.2;
export const VOICE_GAP_SECONDS = 2.2;
export const VOICE_TAIL_SECONDS = 2.6;

/** Durée parlée de chaque séquence, en secondes. */
export const VOICE_DURATIONS = [22, 30, 32, 30, 26];
