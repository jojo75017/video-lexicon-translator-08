import React from 'react';
import KdpCoverStudio from '@/components/ebook/KdpCoverStudio';

/**
 * Module cockpit V3 — Couverture KDP Exacte (PDF).
 * Réutilise le studio de couverture KDP existant (PDF wrap complet : 1re + dos + 4e).
 */
const CoverPdfExact: React.FC = () => <KdpCoverStudio />;

export default CoverPdfExact;
